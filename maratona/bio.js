const alfabeto = 'ABCDEFGHIJKLMNOPQRSTUVWXYZ'.toLowerCase()
const lines = ['adbbabdcdbcbacdabbaccdac'.toLowerCase(), '']
let soma = 0


lines[0].split('').forEach(element => {
    if()
    soma += alfabeto.indexOf(element) + 1
});

console.log(soma)